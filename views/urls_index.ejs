<!DOCTYPE html>
<html lang="en">
<% include ./partials/_header %>
<head>
  <title>TinyApp</title>
</head>
<body>
  <% for(url in urls) { %>
  <%= url %> : <%= urls[url]%><br>
  <% } %>
</body>
</html>
